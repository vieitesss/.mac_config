create a cmux workspace per issue with:
- a title like "<app-name> <issue number>"
- a worktree with a new branch for that issue placed at "~/personal/<app-name>-<something)"
- "opencode" executed in the default terminal of each workspace created for each issue
